<Page x:Class="RevitPluginInstaller.Views.Pages.UpdatePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RevitPluginInstaller.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UpdatePage">
    
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10">
            <TextBlock Text="{Binding ViewModel.Heading}" HorizontalAlignment="Center" />
        </StackPanel>

        <!--<StackPanel Grid.Row="1" Margin="10"> 

        </StackPanel>

        <StackPanel Grid.Row="2" Margin="10">
            <Button Content="Выбрать" Command="{Binding ViewModel.SelectVersionCommand}" CommandParameter="{x:Type pages:SettingPage}" />
        </StackPanel>-->
    </Grid>
</Page>
